import { createAsyncThunk, createSlice } from "@reduxjs/toolkit";
import {
  deleteFromFavourite,
  addToFavourite,
  getFavourite,
} from "../../api/index,.js";

export const favouriteSlice = createAsyncThunk({
    
})